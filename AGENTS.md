# 项目协作原则

## 第一目标：以后端系统设计能力为核心

本项目的首要目标，是通过真实、可运行、可验证的工程实践，锻炼并体现系统设计能力，以及对分布式系统、微服务架构、消息队列、缓存等后端核心技术的理解与应用。

因此，后端是本项目的重中之重。产品界面与 AI 能力必须服务于后端工程目标，不能把项目实现成一个以页面展示、Prompt 编排或第三方模型调用为主的轻量 Demo。

在需求分析、架构设计、任务拆分、实现和审查中，应优先考虑：

- 清晰的领域边界、服务职责与演进路径；
- 分布式状态、一致性、幂等、并发、事务与故障恢复；
- 消息队列的事件模型、投递语义、重试、死信、顺序与背压；
- 缓存的数据模型、失效策略、一致性、热点与穿透治理；
- 工作流编排、异步任务、可观测性、审计链路和可追溯产物；
- 安全隔离、资源治理、弹性扩缩容与性能验证；
- 用真实测试、故障注入、指标和运行证据证明设计，而非只在文档中声称使用了某项技术。

不要为了展示技术而强行拆分微服务或引入中间件。架构复杂度必须对应真实的问题、容量假设和可验证的权衡；项目可以从模块化单体起步，但应保留清晰的服务化边界和可演进路径。

## 产品范围

项目提供一套面向前后端交付的解决方案，覆盖三类核心能力：设计、研发、审查。

- 设计：从需求、Figma 文稿或静态 HTML 出发，以版本化 HTML 产物作为交付事实载体；支持 UI/UX 批注、编辑诉求和 Agent 执行，并将结果回写为新的可审计 HTML 版本。
- 研发：从需求、可预览与裁切的文档及关联 UI 产物出发，支持代码生成、代码审查、在线预览和 PR 交付。
- 审查：基于明确标准和 CR 规则库审查指定代码，既可作为上述流程的内置流水线触发，也可通过 GitHub Actions 等外部系统的 Webhook 触发。

## 工程判断原则

- 区分“模型生成内容”与“系统保证正确性”的边界；关键约束应由 Schema、状态机、权限、验证器和质量门禁保证。
- HTML 可以是设计协作阶段的唯一事实载体，但应通过不可变版本、内容哈希、批注锚点和变更血缘保证可追溯性，避免直接覆盖造成事实丢失。
- 所有长耗时 Agent 任务都按可恢复的异步作业设计，明确状态、租约、超时、取消、重试、幂等键和失败产物。
- 审查能力应建设为可扩展的 Review Harness：范围识别、规则映射、分批执行、结果验证、去重排序、结构化产物和质量门禁，而非单次 Prompt 调用。
- 每次技术选型都要说明它解决的具体问题、替代方案、代价、容量假设和验证方式。

## 技术基线

- 前端使用 Vue 3 + TypeScript；默认采用 Composition API，不使用 Options API 编写新业务模块。
- 后端使用 ExpressJS + TypeScript。Express 只负责 HTTP 适配和中间件编排，业务规则不得直接堆积在路由处理器中。
- 首期采用模块化单体 Control Plane，并将 Worker 与 Sandbox Runner 作为独立进程或部署单元；只有出现独立扩缩容、安全域、故障域或数据所有权证据后，才拆分新的微服务。
- PostgreSQL 是业务状态的权威事实源；大型 Artifact 存入对象存储。消息队列、缓存和其他基础设施必须按问题与 ADR 引入。

## 代码质量是产品能力

本项目自身的代码质量必须达到可以被本项目审查规则严格审查的标准。不得出现“对外审查代码、对内依赖混乱”的双重标准。代码质量、架构一致性和长期可维护性属于交付功能的一部分，不是开发结束后的清理工作。

### 设计与依赖原则

- 坚持高内聚、低耦合：一个模块只拥有一组明确的业务能力和数据边界；跨模块协作通过公开用例、端口或领域事件完成，不得读取对方内部实现或直接操作对方数据表。
- 依赖方向固定为 `interface/adapters → application → domain`。Domain 不得依赖 Express、数据库、消息队列、第三方 SDK 或其他基础设施。
- Express 路由处理器必须保持轻薄，只负责解析请求、调用 Application Use Case、映射响应；事务、权限决策、状态迁移和业务规则必须位于明确的应用层或领域层。
- 基础设施通过显式接口和组合根注入。禁止使用隐藏全局状态、Service Locator、跨模块单例或在业务代码中随处读取环境变量。
- 禁止循环依赖。共享代码只有在形成稳定抽象后才能进入公共包，不能以 `utils`、`helpers` 为名堆积无归属逻辑。
- API、消息、Artifact 和领域事件必须有版本化 Schema，并在系统边界进行运行时校验；不得只依赖 TypeScript 的编译期类型。
- 错误必须使用稳定错误码和类型化错误模型。禁止静默吞错、用 `console.log` 代替结构化日志，或把底层异常和 Secret 直接返回给调用方。
- 数据库约束、事务和幂等不能只靠代码约定；能够由唯一键、外键、检查约束、乐观锁或事务保证的规则，应由数据库共同保证。

### TypeScript 与可读性要求

- TypeScript 必须启用严格模式，并启用 `noUncheckedIndexedAccess`、`exactOptionalPropertyTypes`、`useUnknownInCatchVariables` 等能够提前暴露不安全假设的检查。
- 新代码禁止无理由使用 `any`、非空断言、`@ts-ignore`、大范围 ESLint Disable 或不安全类型转换。确需绕过时，必须把范围缩到最小并注释原因、风险和移除条件。
- 命名必须体现领域含义；禁止用 `data`、`item`、`handle`、`process` 等模糊名称承载核心业务语义。
- 函数和类应保持单一职责。出现过深分支、超大文件、重复逻辑或难以描述职责时，应先重构边界再继续叠加功能。
- 注释用于解释约束、权衡和非显然原因，不复述代码表面行为。所有 `TODO`、`FIXME` 必须关联明确的计划项或问题编号。

### 测试与质量门禁

- 新功能和缺陷修复必须包含与风险匹配的测试。状态机、安全、事务、幂等、重试、缓存失效和外部副作用必须覆盖失败路径，不能只测 Happy Path。
- 全局单元测试覆盖率目标为 lines/statements/functions 不低于 85%、branches 不低于 80%；Identity、Security、Workflow、Review 核心领域的 branches 不低于 90%。覆盖率只是最低门槛，不能用无断言或低价值测试凑数。
- 架构测试必须阻止非法跨层依赖、跨模块内部引用和循环依赖。
- 每个可交付变更在相关脚本建立后，至少通过：格式检查、Lint、TypeScript 类型检查、单元测试、相关集成测试、架构检查、Build 和 `git diff --check`。
- 数据库变更必须验证从空库迁移成功、约束生效和关键兼容路径；消息与 Worker 变更必须验证重复投递、进程崩溃、重试和旧 Attempt 写入等失败场景。
- 不得把 Mock、模型自述、页面可打开或单次 Build 成功当成端到端完成证据。验证结论必须注明实际达到的层级和仍未覆盖的真实环境风险。

### 自审查与交付纪律

- 项目具备可用 Review Harness 后，必须将本仓库作为首个 Dogfooding 目标；规则库、架构边界和质量门禁应持续审查本项目自身。
- 代码生成完成后，必须先执行确定性验证，再进行代码审查，最后才允许准备 PR；审查结束后不重复运行无关 Build/Test，除非是验证审查所指出的问题。
- Code Review 优先检查正确性、安全、数据一致性、并发、兼容性和缺失测试，然后才是格式与风格。阻塞问题未解决时不得标记可交付。
- 不以临时补丁绕过架构约束。若现有边界无法承载新需求，应先补充 ADR、迁移方案和回滚策略。
